# Security Policy

BloomStreak 重视用户习惯记录、认证流程和同步数据的安全。请负责任地报告潜在漏洞，并避免让公开信息扩大风险。

## 支持范围

项目已提供公开在线 Demo（默认使用 LocalStorage 模式），但尚未发布正式 Release。安全修复以默认分支上的最新代码为主要支持对象；旧提交、个人 fork、第三方部署和未合并改动不保证获得补丁。

## 私密报告漏洞

**请不要在公开 Issue、Discussion、Pull Request、社交媒体或其他公开渠道披露漏洞细节。**

仓库创建并启用 GitHub Private Vulnerability Reporting 后，请优先通过仓库的 **Security** 页面提交私有漏洞报告。

如果私有漏洞报告入口尚未启用，且仓库中还没有公布其他私有安全联系方式：

1. 请先保留漏洞细节、复现代码、截图和可能涉及的数据，不要公开提交。
2. 等待维护者在仓库中提供私有报告渠道后再发送完整信息。
3. 不要猜测或使用未经项目确认的邮箱地址。

当前没有对外声明专用安全邮箱。

## 报告建议包含

在私有渠道可用后，请尽量提供：

- 受影响的页面、模块、提交或环境
- 漏洞类型和潜在影响
- 最小可复现步骤或经过脱敏的概念验证
- 是否需要特定 Supabase、浏览器或认证配置
- 已采取的临时缓解措施（如有）
- 你希望使用的署名方式，或是否希望匿名

请删除报告中的真实凭据、Magic Link、访问令牌、个人习惯数据和其他不必要的敏感信息。

## 处理与披露

维护者会在能力范围内确认、复现、评估并修复报告，但发布前项目暂不承诺固定响应时限或修复 SLA。请在维护者确认修复并就披露时间达成一致前保持漏洞细节私密。

## 安全研究边界

请勿：

- 访问、修改、下载或删除不属于你的数据
- 进行拒绝服务、资源耗尽或大规模自动化攻击
- 使用社会工程、钓鱼或骚扰手段
- 破坏真实用户环境或第三方服务
- 在未经授权的生产系统上扩大验证范围

建议在本地环境、你自己控制的 Supabase 测试项目和虚构数据上进行最小化验证。

## 部署者责任

自行部署 BloomStreak 时，请至少：

- 保护所有服务端秘密，绝不把 service role key 暴露给浏览器
- 正确配置 Supabase RLS、认证回调地址和允许的站点 URL
- 使用 HTTPS，并保持 Node.js、Next.js、Supabase SDK 和其他依赖更新
- 建立适合自身环境的备份、日志、监控和事件响应流程

本政策不表示当前已经存在正式的商业托管服务或已发布 Release；在线 Demo 主要用于功能体验。
